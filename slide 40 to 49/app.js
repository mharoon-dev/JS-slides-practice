let person  = {
    name: "haroon",
    grade:10,
    age: 17
}

console.log(person.name);
console.log(person.name.length + " ==> length of name");
console.log(`${person.name.charAt(5)} ==> charAt 5`);
console.log(person.name.indexOf("n"));
console.log(person.name.toLowerCase());
console.log(person.name.toUpperCase());
console.log(person.name.includes("aroon"));
console.log(person.name.startsWith("ali"));
console.log(person.name.slice(0 , 3));